# Prompty — ส่งมอบการแก้ก่อน User Testing

วันที่: 23 กันยายน 2026
โฟลเดอร์: `D:\Code\Prompty\prompty`
Branch: `fix/user-testing-readiness`

## แก้แล้ว

1. รีเซ็ตรหัสผ่านต้องมีหลักฐาน OTP ที่เซิร์ฟเวอร์ออกให้ ใช้ครั้งเดียว หมดอายุได้ และผูกกับอีเมล เก็บหลักฐานใน HttpOnly cookie; ใช้ธุรกรรมตอนเปลี่ยนรหัสผ่าน
2. OTP ใช้ตัวสุ่มสำหรับงานความปลอดภัย เก็บเป็น HMAC แยกวัตถุประสงค์สมัคร/รีเซ็ต และป้องกันการใช้ซ้ำ
3. การตั้งค่าผู้ใช้รับเฉพาะฟิลด์ที่อนุญาตและตรวจค่าจริง ปิดช่องเปลี่ยน role/password/MFA ผ่านข้อมูลแฝง
4. MFA ผูกกับ session ID ที่คงที่ ไม่ใช้ JWT iat หรือ timestamp ร่วมทั้งบัญชี ตรวจสิทธิ์ไม่สำเร็จแล้วปฏิเสธแทนการปล่อยผ่าน
5. ตั้งค่า MFA ต้องใช้ secret ที่เซิร์ฟเวอร์ออกให้ ไม่สามารถเขียนทับ MFA ที่เปิดอยู่; Backup Code ใช้ครั้งเดียวภายใต้ transaction และป้องกัน TOTP replay
6. ตรวจ session/MFA/สถานะบัญชี/maintenance ภายใน Server Actions และ API ที่เกี่ยวข้อง ไม่พึ่งการเปลี่ยนหน้าของ proxy อย่างเดียว
7. อัปโหลดต้องมีสิทธิ์ ตรวจขนาดและลายเซ็นชนิดไฟล์ จำกัดคำขอ และแยกชื่อไฟล์ตามผู้ใช้; ย้ายฟังก์ชันสร้าง notification ภายในออกจาก Server Actions ที่เรียกจาก client ได้
8. Bookmark ตรวจเจ้าของคอลเลกชันปลายทาง
9. เพิ่มตัวจำกัดคำขอแบบ atomic ใน PostgreSQL สำหรับ OTP/MFA/ล็อกอิน/AI/อัปโหลด/เปลี่ยนรหัสผ่าน และ copy tracking
10. AI ตรวจชนิด/ขนาดข้อมูล ตอบการจำกัดคำขอพร้อมเวลารอเมื่อแอปรู้ค่า และไม่ยอมรับผล Enhance ที่ถูกตัดเพราะ output token limit
11. เปลี่ยน/รีเซ็ตรหัสผ่านแล้ว session เดิมใช้ต่อไม่ได้ ลดข้อมูลละเอียดอ่อนที่ Admin Actions ส่งกลับ
12. หน้าล็อกอินใช้ Enter ได้ มี label ผูกกับ input และนำปุ่ม OAuth ที่ยังไม่ทำงานออก หน้าตั้งค่ามีการจัดการข้อผิดพลาดแทนการค้างที่ปุ่มบันทึก

## ผลตรวจ

| การตรวจ | ผล |
|---|---|
| Production build | ผ่าน หลังแก้ล่าสุด |
| TypeScript | ผ่าน |
| Regression tests | 19/19 ผ่าน รันด้วย `npm test` |
| การจำกัดคำขอ | ทดสอบ SQL จริงใน PostgreSQL ผ่าน PGlite แบบ in-memory; ส่ง 20 คำขอด้วยเพดาน 5 ยอมรับ 5 และปฏิเสธ 15 |
| HTTP smoke test | 11 กรณีผ่าน: หน้าสาธารณะ 5 หน้า, API ต้องล็อกอิน 5 เส้นทางคืน 401, หน้า settings เปลี่ยนไป login |
| ตรวจหน้าจอ | หน้าล็อกอินและลืมรหัสผ่านใน browser; ตรวจหน้าล็อกอินใหม่หลัง build |
| Git diff check | ผ่าน |
| ESLint ทั้งโปรเจกต์ | ยังมีรายการเดิม 30 errors และ 76 warnings ลดจาก 56/77; ส่วนที่เหลือเป็น any และ React effects ในโค้ดเดิม ไม่ได้ปิดกฎของแอปเพื่อซ่อนปัญหา |

Regression tests โหลดฟังก์ชันจริง โดยจำลองฐานข้อมูล/auth/email เพื่อไม่แตะบัญชีจริง การทดสอบคำขอพร้อมกันใช้ transaction จำลองที่เรียงงาน ส่วน SQL rate limit รันกับ PostgreSQL จริงแบบ single connection ของ PGlite จึงยังไม่ใช่การทดสอบหลาย instance บน Vercel

## ผลต่อระบบเดิม

- ไม่มีการแก้ schema หรือรัน migration; ใช้ VerificationToken และ SystemSetting ที่มีอยู่แล้ว
- ผู้ใช้ต้องล็อกอินใหม่หลังอัปเดต และ OTP ค้างก่อนอัปเดตต้องขอใหม่
- ไม่ได้เปลี่ยน AUTH_SECRET หรือค่าจริงใน .env และไม่ได้ส่งอีเมล/เรียก Gemini/อัปโหลด Storage เพื่อทดสอบ
- ไม่ได้เผยแพร่ไป Vercel และยังไม่ได้เปลี่ยนเว็บจริง
- สามารถกำหนด GEMINI_MODEL เพื่อเลือกโมเดลที่บัญชีมีสิทธิ์ใช้ได้; ค่าเริ่มต้นเดิมยังอยู่ ไม่ได้ยืนยันโควตาจริงจากบัญชีผู้ให้บริการ
- บันทึก rate limit ใช้ key นำหน้าด้วย `__rate:` และไม่แสดงรวมใน Admin settings; ข้อมูลหมดอายุใน VerificationToken และ rate keys ควรมีงานล้างตามนโยบายเมื่อระบบเติบโต

## สิ่งที่ต้องทดสอบก่อนเปิดให้เพื่อนใช้ลิงก์จริง

ใช้รายการใน `Prompty-final-test-checklist.md` ทดสอบอีเมลจริง, Supabase Storage, Gemini quota และเส้นทางหลังล็อกอินด้วยบัญชีทดสอบบน deployment เป้าหมาย งานรอบนี้ไม่ได้ทดสอบครบทุกหน้าด้วยบัญชีจริงและไม่ใช่การรับรองว่าปราศจากบั๊กทั้งหมด

สำหรับนักพัฒนา: `npm test`, `npm run build`, `npm run dev` เป็นคำสั่งหลัก ไม่ต้องเปลี่ยนฐานข้อมูลเพื่อเริ่มใช้การแก้ชุดนี้
