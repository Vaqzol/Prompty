# Prompty — รายการทดสอบรอบสุดท้าย

เวอร์ชัน: branch `fix/user-testing-readiness` · 23 กันยายน 2026

**ต้องทดสอบเวอร์ชันที่แก้แล้ว** งานยังอยู่ในเครื่องและยังไม่ได้เผยแพร่ไป Vercel ลิงก์เว็บเดิมจึงอาจยังเป็นเวอร์ชันก่อนแก้ ให้เปิดโฟลเดอร์ `D:\Code\Prompty\prompty` บน branch นี้ หรือสร้าง deployment จาก branch นี้ก่อนส่งลิงก์ให้เพื่อน

หลังอัปเดต ผู้ใช้ทุกคนต้องล็อกอินใหม่ รหัส OTP ที่ขอก่อนอัปเดตต้องขอใหม่ การเปลี่ยนหรือรีเซ็ตรหัสผ่านจะยกเลิก session เก่าโดยอัตโนมัติ

## เตรียมทดสอบ

- ใช้บัญชีทดสอบ A และ B ที่เข้าถึงอีเมลได้ และบัญชี Admin ที่มีอยู่สำหรับทดสอบส่วนผู้ดูแล
- ใช้รูป PNG/JPEG/WebP ขนาดเล็ก ข้อความ Prompt และ Code ตัวอย่างที่ไม่มีข้อมูลส่วนตัว
- เปิดสอง browser profiles หรือ browser ปกติคู่กับ Incognito เพื่อแยก session จริง
- บันทึกชื่อ browser/อุปกรณ์ ขั้นตอน ผลที่คาด ผลจริง และภาพหน้าจอเมื่อพบปัญหา
- แนะนำ staging ที่ใช้ข้อมูลทดสอบ โดยเฉพาะกรณีแบน/ลบบัญชีและลบโพสต์

## กรณีทดสอบ

| ผ่าน | ส่วน | ขั้นตอนและผลที่ต้องได้ |
|---|---|---|
| ☐ | สมัครสมาชิก | สมัครด้วยอีเมลใหม่ รับ OTP และยืนยันสำเร็จ; OTP ผิด/ใช้ซ้ำต้องไม่ผ่าน |
| ☐ | ส่ง OTP ใหม่ | กดซ้ำภายในหนึ่งนาทีต้องถูกจำกัด; หลังรอแล้วขอใหม่ได้ |
| ☐ | ล็อกอิน | รหัสถูกเข้าสู่ระบบได้; รหัสผิดแสดงข้อผิดพลาด; กด Enter ในฟอร์มใช้งานได้ |
| ☐ | ลืมรหัสผ่าน | ขอ OTP → ยืนยัน → ตั้งรหัสใหม่ → ล็อกอินด้วยรหัสใหม่ได้; รหัสเดิมใช้ไม่ได้ |
| ☐ | ข้าม OTP | เปิดหน้าตั้งรหัสผ่านใหม่โดยไม่ยืนยัน OTP ก่อน ต้องเปลี่ยนรหัสไม่ได้ |
| ☐ | MFA ตั้งค่า | สแกน QR ยืนยันรหัสและบันทึก Backup Codes; ควรใช้งานต่อได้หลังตั้งค่าสำเร็จ |
| ☐ | MFA แยก session | ล็อกอินบัญชีเดียวกันใน A/B; ยืนยัน MFA ที่ A แล้ว B ยังต้องยืนยันเอง |
| ☐ | MFA refresh | หลังผ่าน MFA แล้ว reload หน้าและเปิดหน้าอื่น ต้องไม่วนกลับมาขอ OTP โดยไม่มีเหตุ |
| ☐ | MFA รหัสซ้ำ | รหัส TOTP เดิมไม่ควรยืนยันอีก session ได้ทันที; รอรหัสรอบใหม่แล้วลอง |
| ☐ | Backup Code | ใช้ได้หนึ่งครั้ง ครั้งถัดไปต้องไม่ผ่าน; รหัสชุดอื่นยังใช้งานได้ |
| ☐ | ปิด MFA | ต้องใช้รหัสผ่านและ OTP ที่ถูกต้อง; เมื่อล็อกอินใหม่ไม่ต้องกรอก MFA |
| ☐ | โพสต์ | สร้าง Prompt/Code แก้ไข ลบ และเปิดรายละเอียดได้; B แก้/ลบโพสต์ของ A ไม่ได้ |
| ☐ | อัปโหลด | อัปโหลดรูปจริงได้ทั้ง avatar และรูปโพสต์; ไฟล์ชนิดอื่น/เกินขนาดถูกปฏิเสธ |
| ☐ | AI | Enhance และ Suggest Tags ทำงาน; เมื่อโควตาหมดมีข้อความที่เข้าใจได้ และยังโพสต์โดยไม่ใช้ AI ได้ |
| ☐ | โหวต/คอมเมนต์ | โหวตและยกเลิกได้ แสดงความคิดเห็นและเห็นผลที่ถูกต้อง |
| ☐ | Bookmark | สร้าง/เปลี่ยนชื่อ/ลบคอลเลกชัน ย้ายโพสต์ และแชร์คอลเลกชันสาธารณะได้; private เห็นเฉพาะเจ้าของ |
| ☐ | Follow/Notifications | ติดตามและยกเลิกติดตามได้; การแจ้งเตือนและการอ่านแล้วทำงานตามการตั้งค่า |
| ☐ | ค้นหา/จัดอันดับ | ค้นหา แท็ก หมวดหมู่ Trending และ Leaderboard เปิดได้; ข้อมูลที่มี cache อาจยังไม่เปลี่ยนทันที |
| ☐ | การตั้งค่า | เปลี่ยนชื่อ โปรไฟล์ ธีม และการแจ้งเตือนแล้วบันทึก/โหลดใหม่ได้ |
| ☐ | เปลี่ยนรหัสผ่าน | รหัสเดิมผิดต้องไม่ผ่าน; เปลี่ยนสำเร็จแล้วต้องล็อกอินใหม่ รวมถึง session อีกหน้าต่าง |
| ☐ | Admin | ผู้ใช้ธรรมดาเข้าหน้า Admin ไม่ได้; Admin จัดการผู้ใช้/โพสต์/รายงาน/แท็กได้ |
| ☐ | แบนบัญชี | session ที่ล็อกอินอยู่ของผู้ถูกแบนต้องทำรายการต่อไม่ได้ |
| ☐ | Maintenance | ผู้ใช้ทั่วไปถูกกั้น; Admin ยังจัดการได้; ปิดแล้วผู้ใช้ใช้งานต่อได้ |
| ☐ | มือถือ | ทดสอบฟอร์ม เมนู modal โพสต์ และคีย์บอร์ดบนมือถือจริง |

## ข้อจำกัดที่เป็นพฤติกรรมตั้งใจ

- ลอง MFA/OTP อีเมลได้สูงสุด 10 ครั้งต่อบัญชีใน 15 นาที การลองถูกก็นับรวมด้วย
- ขออีเมล OTP ได้หนึ่งครั้งต่อนาทีและห้าครั้งต่อชั่วโมงต่ออีเมล มีเพดานรวม 100 ครั้งต่อชั่วโมงเพื่อป้องกันการส่งอีเมลมากผิดปกติ
- AI จำกัดในแอปห้าครั้งต่อนาทีและ 40 ครั้งต่อวันต่อผู้ใช้ แต่ผู้ให้บริการอาจจำกัดต่ำกว่านี้ตามโควตาบัญชี
- ปุ่ม Google/GitHub ถูกนำออก เพราะระบบยังไม่ได้ตั้งค่า OAuth; ใช้อีเมลและรหัสผ่าน
- ยังไม่มีหน้า Admin reset MFA อัตโนมัติ ให้เก็บ Backup Codes ก่อนทดสอบ MFA

## แบบบันทึกบั๊ก

- หัวข้อ:
- อุปกรณ์ / browser:
- หน้าที่เกิดปัญหา:
- ขั้นตอนทำซ้ำ:
- ผลที่คาดหวัง:
- ผลจริง:
- ภาพหน้าจอ / ข้อความผิดพลาด:
- เกิดทุกครั้งหรือบางครั้ง:

ห้ามใส่รหัสผ่าน, OTP, Backup Codes หรือ API keys ในรายงานบั๊ก
